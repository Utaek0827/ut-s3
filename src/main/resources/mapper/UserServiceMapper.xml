<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.uts3back.mapper.UserServiceMapper">

    <select id="userServiceFind" resultType="com.uts3back.dto.UserServiceDTO">
        SELECT * FROM UserService WHERE email = #{email}
    </select>

</mapper>
